# Используйте официальный образ Python в качестве родительского образа
FROM python:3.9-slim

# Установите рабочий каталог в контейнере
WORKDIR /app

# Копирование содержимого текущей директории в контейнер в /app
COPY . /app

# Установите необходимые пакеты, указанные в requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Сделайте порт 80 доступным в мире вне этого контейнера
EXPOSE 80

# Запустите app.py при запуске контейнера
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "80"]
